import React    from "react";
import template from "./Navigationbar.jsx";

class Navigationbar extends React.Component {
  render() {
    return template.call(this);
  }
}

export default Navigationbar;
